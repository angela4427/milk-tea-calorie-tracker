# V1 原型

墨刀里做的 V1 界面稿，本地存档放在这里。

## 怎么看

- **推荐**：直接打开 [`modao-preview.png`](./modao-preview.png)，这是完整页面截图。
- **网页存档**：[`墨刀-奶茶热量追踪原型_files/`](./墨刀-奶茶热量追踪原型_files/) 是浏览器「另存为」留下的资源目录。

## 说明

这次存档不完整：

1. 没有配套的 `.html` 入口文件，单靠 `_files` 目录没法在浏览器里完整打开。
2. 目录里不少 JS 仍是 `.下载` 后缀，属于下载中断留下的半成品。
3. 因此仓库里以 `modao-preview.png` 作为可查阅的正式原型图；`_files` 目录仅作原始素材保留。

如果后面有完整的墨刀分享链接，或重新导出的 PNG，可以补到这个目录，并在根目录 README 里更新。
